name: Artifact Workflow
on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@v4

      - name: Restore Maven Cache
        id: m2-cache
        uses: actions/cache@v6
        with:
          path: m2-cache
          key: m2-cache-${{ hashFiles('pom.xml') }}
          restore-keys: |
            m2-cache-

      - name: Inject Maven cache into Docker build
        uses: reproducible-containers/buildkit-cache-dance@v3
        with:
          builder: ${{ steps.setup-buildx.outputs.name }}
          cache-dir: m2-cache
          dockerfile: Dockerfile
          skip-extraction: ${{ steps.m2-cache.outputs.cache-hit }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          target: output
          outputs: type=local,dest=out
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: output
          path: out

# ======== EXAMPLE ======== #
#  example deploy code that you might write to deploy a jar file to a remote server
#  deploy:
#    needs: build
#    if: github.event.name != 'pull_request'
#    runs-on: [ 'self-hosted', 'linux', 'jump-box' ]
#    container:
#       image: ubuntu:latest
#    environment: production
#    steps:
#      - name: Download Artifact
#        uses: actions/download-artifact@v7
#        with:
#          name: output
#          path: out
#
#      - name: Set up SSH
#        shell: bash
#        run: |
#          mkdir -p ~/.ssh
#          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/id_rsa
#          chmod 600 ~/.ssh/id_rsa
#
#      - name: Deploy jar and restart service
#        run: |
#          scp -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa \
#            out/java-demo.jar \
#            ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/opt/apps/java-demo.jar
#
#          ssh -o StrictHostKeyChecking=no -i ~/.ssh/deploy_key \
#            ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} \
#            "sudo systemctl restart java-demo"
